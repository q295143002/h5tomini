
    <view class="anu-block">
        <view class="anu-page-header">{{state.title}}</view>
        <block wx:for="{{state.array}}" wx:for-item="item" wx:for-index="i1299">
            <view class='anu-item'>
                <view bindtap="dispatchEvent" open-type="navigate" hover-class="navigator-hover" url="{{item.url}}" data-tap-uid="{{'e50_32_' + i1299}}" data-beacon-uid="default">{{item.name}}</view>
            </view>
        </block>
    </view>
